const account_Id = 144553
let accountEmail = "saniafida261@gmail.com"
var accountpassword = "12345"
accountCity = "pak"

//accountId = 2 // not allowed

accountEmail = "hc@hc.com"
accountpassword = "21212121"
accountCity = "Danyor"
let accountstate;

console.log(account_Id);

// Use `const` when the value shouldn't change.
// Use `let` when the value will change.
// Avoid `var` because it can cause issues with block and function scope.


console.table([account_Id, accountEmail, accountpassword, accountCity, accountstate])
